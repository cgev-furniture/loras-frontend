// ConfirmModal — reusable delete confirmation dialog.
// Props: isOpen, title, message, onConfirm, onCancel, isLoading
// role="dialog", aria-modal="true", focus trapping, Escape to close.
// Used in admin ProjectList for delete confirmations.
export default function ConfirmModal() {
  return null;
}
