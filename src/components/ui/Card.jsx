// Card — project card component.
// Props: project { id, slug, title, thumbnailUrl, categories, featured }
// Thumbnail aspect ratio 4:3, object-fit: cover.
// Shows title and category tag chips.
// Wraps in a Link to /portfolio/:slug.
export default function Card() {
  return null;
}
