// ImageFallback — wraps <img> and shows a grey branded placeholder
// if the GCS image fails to load (onError handler).
// Props: src, alt, className, width, height, loading
// Falls back to a light grey rectangle with a subtle LORAS mark SVG.
export default function ImageFallback() {
  return null;
}
