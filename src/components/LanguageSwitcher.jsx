// LanguageSwitcher — hy / ru / en toggle buttons.
// Changes i18next language, persists selection to localStorage,
// and updates document.documentElement.lang attribute.
// Shown in Navbar (desktop) and inside mobile drawer.
export default function LanguageSwitcher() {
  return null;
}
