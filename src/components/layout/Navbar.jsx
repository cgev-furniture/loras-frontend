// Navbar — sticky header with transparent-to-solid scroll transition,
// logo (horizontal light variant), nav links, language switcher,
// and a mobile hamburger drawer containing links + language switcher.
export default function Navbar() {
  return null;
}
