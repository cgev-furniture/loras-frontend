// Admin Dashboard — /admin
// Stats from GET /api/v1/admin/stats:
//   total published projects, total drafts, projects per category,
//   total contact inquiries, unread inquiries count.
// Quick-action buttons: "Add project" → /admin/projects/new,
//   "View inquiries" → /admin/inquiries.
// All text via useTranslation('admin')
export default function Dashboard() {
  return null;
}
