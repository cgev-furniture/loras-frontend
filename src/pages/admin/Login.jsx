// Admin Login page — /admin/login
// Form: username + password fields, submit button.
// On success: JWT cookie set by backend; redirect to /admin (or preserved destination).
// On error: show inline error message (translated via admin.json).
// First login with default credentials forces password-change screen.
// <meta name="robots" content="noindex, nofollow"> via react-helmet-async.
// All text via useTranslation('admin')
export default function Login() {
  return null;
}
