// Admin CategoryManager — /admin/categories
// Lists all categories with project counts.
// Actions: Add new category, Rename inline, Delete (with ConfirmModal).
// All text via useTranslation('admin')
export default function CategoryManager() {
  return null;
}
