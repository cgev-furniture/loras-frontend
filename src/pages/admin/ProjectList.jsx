// Admin ProjectList — /admin/projects
// Server-side paginated table (12/page).
// Columns: thumbnail, title, categories, status badge (Published/Draft),
//   created date (sortable).
// Per-row: inline publish/unpublish toggle, Edit link, Delete (ConfirmModal).
// Search/filter by title or category.
// All text via useTranslation('admin')
export default function ProjectList() {
  return null;
}
