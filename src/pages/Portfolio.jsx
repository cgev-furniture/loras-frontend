// Portfolio page — paginated project grid with category filter bar.
// Filter chips: horizontally scrollable, multi-select, aria-pressed.
// Filter & pagination state stored in URL via useSearchParams.
//   e.g. ?page=2&categories=1,3
// 12 projects per page. Skeleton loader (12 × SkeletonCard) while loading.
// Zero-results state with "View all" CTA.
// SEO: react-helmet-async title + description via useTranslation('portfolio')
export default function Portfolio() {
  return null;
}
