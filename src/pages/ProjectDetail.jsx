// ProjectDetail page — route: /portfolio/:slug
// Displays: title, full description, category tags, formatted date,
// image gallery (yet-another-react-lightbox), optional video player.
// Gallery: thumbnails in grid, clicking opens lightbox with keyboard nav,
//   swipe on mobile, focus trapping (role="dialog", aria-modal, aria-label).
// All <img> use loading="lazy" except the first hero image.
// Video: <video controls preload="none" playsinline poster={firstImageUrl}>
// "Back to portfolio" button preserving previous filter/page URL state.
// OG meta tags via react-helmet-async.
export default function ProjectDetail() {
  return null;
}
