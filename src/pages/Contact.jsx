// Contact page
// Headline: "Let's Build Something Together" (translated)
// Form fields: Name (required), Email (required+validated), Phone (optional),
//   Message (required, max 2000 chars with live counter).
// Honeypot: hidden <input name="website" tabIndex={-1} aria-hidden="true">
// Validation: react-hook-form + zod, fires on blur and submit.
// Submit: button disabled immediately to prevent double-submission.
// Success: form replaced with warm confirmation message.
// Error: inline error message.
// Contact panel: WhatsApp (most prominent), phone, email, Instagram, location.
// SEO: react-helmet-async title + description.
// All text via useTranslation('contact')
export default function Contact() {
  return null;
}
